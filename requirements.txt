# GPU 사용 시 torch는 아래 링크에서 CUDA 버전에 맞게 먼저 설치: 
#   pip install torch --index-url https://download.pytorch.org/whl/cu118
#   pip install torch --index-url https://download.pytorch.org/whl/cu121

# ── 시스템 의존성 (pip 아닌 apt로 설치) ──────────────────────
#   sudo apt install ffmpeg
#   sudo apt install openjdk-21-jdk   (Neo4j 실행에 필요)
#   (ffmpeg + ffprobe 모두 포함)

# ── Core & Utilities ──────────────────────────────────────────
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0
python-dotenv>=1.0.0
librosa
pymupdf
python-pptx
ImageHash>=4.3.1
json-repair

# ── AI & API Clients ──────────────────────────────────────────
google-generativeai>=0.8.0
google-genai>=0.8.0
groq

# ── ML & Vision ───────────────────────────────────────────────
torch>=2.0.0
transformers>=4.40.0

# ── Text & Knowledge Graph ────────────────────────────────────
kiwipiepy
pyvis>=0.3.2

# ── Graph DB ─────────────────────────────────────────────────
neo4j>=5.0.0

# ── Web API (질의 시스템) ─────────────────────────────────────
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.0.0