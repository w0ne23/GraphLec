services:
  backend:
    gpus: all
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility,video
      - GRAPHLEC_SLIDE_DECODE_BACKEND=auto
      - GRAPHLEC_FFMPEG_HWACCEL=cuda
