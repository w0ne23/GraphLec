services:
  db:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-graphlec}
      POSTGRES_USER: ${POSTGRES_USER:-user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-pass}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-user} -d ${POSTGRES_DB:-graphlec}"]
      interval: 5s
      retries: 5

  neo4j:
    image: neo4j:5.12
    restart: unless-stopped
    ports:
      - "7474:7474"
      - "7687:7687"
    environment:
      - NEO4J_AUTH=${NEO4J_USER:-neo4j}/${NEO4J_PASSWORD:-password123}
      - NEO4J_PLUGINS=["apoc"]
    volumes:
      - neo4j_data:/data
      - neo4j_logs:/logs
    healthcheck:
      test: ["CMD-SHELL", "cypher-shell -a bolt://localhost:7687 -u ${NEO4J_USER:-neo4j} -p ${NEO4J_PASSWORD:-password123} 'RETURN 1' >/dev/null"]
      interval: 10s
      timeout: 10s
      retries: 12
      start_period: 20s

  query_service:
    build:
      context: .
      dockerfile: ./query_service/Dockerfile
      network: host
    ports:
      - "8001:8001"
    environment:
      - PYTHONUNBUFFERED=1
      - GOOGLE_API_KEY=${GOOGLE_API_KEY:-}
      - GOOGLE_API_KEY_2=${GOOGLE_API_KEY_2}
      - NEO4J_URI=bolt://neo4j:7687
      - NEO4J_USER=${NEO4J_USER:-neo4j}
      - NEO4J_PASSWORD=${NEO4J_PASSWORD:-password123}
      - GRAPHLEC_LANCE_ROOT=/lance/lancedb
    volumes:
      - lance_data:/lance
    depends_on:
      neo4j:
        condition: service_healthy

  backend:
    build:
      context: .
      dockerfile: ./app/backend/Dockerfile
      network: host
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
      - DATABASE_URL=${DATABASE_URL:-postgresql+asyncpg://user:pass@db/graphlec}
      - GOOGLE_API_KEY_1=${GOOGLE_API_KEY_1}
      - GOOGLE_API_KEY_2=${GOOGLE_API_KEY_2}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - GROQ_API_KEY=${GROQ_API_KEY}
      - CROSS_VERIFY_MODEL=${CROSS_VERIFY_MODEL:-gpt-5.4}
      - VERIFIER_MODEL=${VERIFIER_MODEL:-gemini-2.5-flash}
      - VERIFIER_CLAIM_EXTRACT_MODEL=${VERIFIER_CLAIM_EXTRACT_MODEL:-}
      - VERIFIER_CLAIM_JUDGE_MODEL=${VERIFIER_CLAIM_JUDGE_MODEL:-}
      - VERIFIER_CROSS_RECHECK_MODEL=${VERIFIER_CROSS_RECHECK_MODEL:-}
      - VERIFIER_SLIDE_RECHECK_MODEL=${VERIFIER_SLIDE_RECHECK_MODEL:-}
      - VERIFIER_GROUNDING_MODEL=${VERIFIER_GROUNDING_MODEL:-}
      - VERIFIER_TEMPERATURE=${VERIFIER_TEMPERATURE:-0.0}
      - QUERY_SERVICE_URL=http://query_service:8001
      - NEO4J_URI=bolt://neo4j:7687
      - NEO4J_USER=${NEO4J_USER:-neo4j}
      - NEO4J_PASSWORD=${NEO4J_PASSWORD:-password123}
      - GRAPHLEC_LANCE_ROOT=/lance/lancedb
      - LOCAL_STORAGE_DIR=/pipeline/local_storage
      - GRAPHLEC_SLIDE_DECODE_BACKEND=auto
      - GRAPHLEC_METADATA_DIR=/app/metadata
      - RECOMMENDER_DB_DIR=/lance/lancedb
    volumes:
      - ./input:/pipeline/input
      - ./local_storage:/pipeline/local_storage
      - lance_data:/lance
      - ./app/backend:/app
    depends_on:
      db:
        condition: service_healthy
      neo4j:
        condition: service_healthy
      query_service:
        condition: service_started

  recommender:
    build:
      context: .
      dockerfile: ./app/backend/Dockerfile
      network: host
    ports:
      - "8002:8002"
    environment:
      - PYTHONUNBUFFERED=1
      - GOOGLE_API_KEY_2=${GOOGLE_API_KEY_2}
      - METADATA_DIR=/app/metadata
      - RECOMMENDER_DB_DIR=/lance/lancedb
    volumes:
      - ./app/backend:/app
      - lance_data:/lance
    command:
      - /bin/sh
      - -lc
      - python /app/recommender/build_index.py --metadata_dir "$$METADATA_DIR" --db_dir "$$RECOMMENDER_DB_DIR" && uvicorn recommender.recommender_web:app --host 0.0.0.0 --port 8002 --reload
    depends_on:
      - backend

  frontend:
    build: ./app/frontend
    ports:
      - "5173:5173"
    environment:
      VITE_API_BASE_URL: http://localhost:8000
      VITE_RECOMMENDER_PROXY_TARGET: http://recommender:8002
    volumes:
      - ./app/frontend:/app
      - /app/node_modules
    depends_on:
      - backend
      - recommender


volumes:
  pgdata:
  neo4j_data:
  neo4j_logs:
  lance_data:
